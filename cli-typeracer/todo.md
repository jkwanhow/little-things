# Current Todos
[] Need to split words
[] type on each word at a time
[] can't backspace to a previous word
[] can backspace *if* the previous word was wrong
[] if previous word was right and the before that one was wrong, then can't backspace since prev was right
